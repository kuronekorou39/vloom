[package]
name = "vloom-fountain"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Fountain code (RaptorQ) wrapper for video QR streaming"

[lib]
name = "vloom_fountain"
path = "src/lib.rs"

[dependencies]
raptorq = { version = "2", default-features = false }
